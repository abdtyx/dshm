#ifndef _CALCULATE_CPP
#define _CALCULATE_CPP
#endif

#ifndef debug
#define debug false
#endif

#include "../include/stdc++.h"

using namespace std;

void Calculate() {
    if (debug) {
        cout << "This is calculate." << endl;
        return;
    }
}